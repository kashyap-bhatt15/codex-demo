version: "3.9"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      FLASK_APP: run
      FLASK_ENV: production
      DATABASE_URL: sqlite:////data/app.sqlite
    volumes:
      - instance-data:/data
    restart: unless-stopped
volumes:
  instance-data:
